{
  "name": "plva-accelerated-redactor-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "node --check bin/redactor-worker.mjs && node --check src/worker.js && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@huggingface/transformers": "3.7.5",
    "@nationaldesignstudio/rampart": "0.1.3",
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
